int square(float x) {
 return x * x;
}

int vec_main()
{
    squares := @map(square, {1.0,2.0,3.0});
    i := 0;

    while (i < 3) {
     printf("%.1f\n", squares[i]);
     i += 1;
    }

    return 0;
}
