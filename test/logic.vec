int vec_main()
{
    assert(3 == 3);
    assert(1 || 0);
    assert(1 && 1);
    assert(!(0 && 1));
    assert(3 < 5);
    assert(4 > 1);
    assert(2 <= 2);
    assert(3 >= 1);

    printf("All tests passed\n");

    return 0;
}
