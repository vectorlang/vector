int vec_main()
{
    int arr[2048];
    int arr2[2048];

    pfor (i in 0:len(arr)) {
        arr[i] = i;
    }
    pfor (i in 0:len(arr)) {
        arr2[i] = arr[i] * 2;
    }

    for (i in 0:len(arr)) {
        if (!(arr[i] == i && arr2[i] == 2 * i)) {
            printf("incorrect\n");
            return -1;
        }
    }

    printf("correct\n");

    return 0;
}
